<ul>
  <% if Admin::SectionsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_sections_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_section_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::SectionsController.sortable? and Section.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Sections"),
                 admin_sections_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Sections"),
                 admin_sections_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>