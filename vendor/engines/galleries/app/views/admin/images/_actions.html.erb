<ul>
  <% if Admin::ImagesController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_images_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_image_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::ImagesController.sortable? and Image.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Images"),
                 admin_images_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Images"),
                 admin_images_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>